{
  "name": "loan-default-prediction",
  "version": "1.0.0",
  "private": true,
  "description": "LoanShield AI - Loan Default Risk Assessment Platform",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview"
  }
}
